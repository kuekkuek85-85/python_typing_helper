# AGENTS.md

이 저장소에서 작업하는 AI 에이전트(Codex 등)를 위한 안내입니다.

## 프로젝트

중학교 1학년 정보 교과용 파이썬 타자 연습 웹 앱.
Flask + 바닐라 JavaScript + Firebase Firestore.

**작업 전에 `CLAUDE.md`를 읽으세요.** 파일 구조, 개발·테스트 명령, 반드시 지켜야 할
설계 규칙이 그쪽에 정리되어 있습니다. 이 문서는 그 내용을 중복하지 않습니다.

## 개발 명령

```bash
# 의존성
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/bin/pip install pytest

# 테스트 (Firebase 없이 동작)
STORE_BACKEND=local .venv/bin/python -m pytest -q

# 로컬 실행
STORE_BACKEND=local SESSION_SECRET=dev .venv/bin/python main.py
```

응답과 코드 주석은 한국어로 작성합니다.

## Review guidelines

변경분(diff)만 리뷰하고, 범위 밖의 기존 코드는 지적하지 마세요.

### 이 프로젝트에서 실제로 사고가 났던 지점 — 우선 확인

1. **점수 공식 이중화**
   점수는 서버 `scoring.compute_score()`와 화면 `static/js/app.js`의 `computeScore()`가
   **같은 공식**(`round(타수 × (정확도/100)² × 100)`)을 써야 합니다.
   한쪽만 바뀌면 학생이 본 점수와 저장된 점수가 달라집니다.

2. **정상 기록을 거부하는 검증**
   `scoring.py`와 `app.py`의 `create_record`가 정상적으로 연습한 학생의 기록까지
   막을 수 있는지 확인하세요. 과거에 점수 검증식 불일치, 타이핑 구간 계산 오류,
   타수 배율 보정 때문에 **저장이 거의 항상 실패**했습니다. 새 검증을 추가할 때는
   "성실하게 연습한 학생이 이 조건에 걸릴 수 있는가"를 반드시 따지세요.

3. **연습 세션 백엔드**
   `sessions.py`에는 메모리 백엔드와 Firestore 백엔드가 있고, 상태 전이 함수
   (`_apply_start`, `_apply_keystrokes`)를 **공유**합니다. 한쪽 백엔드에만
   규칙이 추가되어 부정행위 판정이 배포 환경에 따라 달라지면 지적하세요.

   메모리 백엔드는 워커/인스턴스가 1개일 때만 동작합니다. 이 가정을 깨는 변경이
   있으면 지적하세요. Firestore 백엔드에서는 토큰 버킷이 읽고-고쳐-쓰는 연산이라
   **트랜잭션 밖에서 수정하면** 동시 요청이 서로의 차감을 덮어씁니다.

4. **비밀 정보**
   Firebase 서비스 계정 키(`*firebase-adminsdk*.json` 등)나 `.env`가 커밋에
   포함됐는지, 로그·API 응답·에러 메시지로 새어 나가는지 확인하세요.

5. **클라이언트 값 신뢰**
   브라우저가 보낸 값을 그대로 저장하거나 신뢰하는 코드가 새로 생겼는지 확인하세요.
   `score`와 `duration_sec`은 **반드시 서버가 계산**해야 합니다.
   키 입력 개수는 `sessions.TypingActivity.credit()`의 토큰 버킷으로 경과 시간에
   맞는 만큼만 인정합니다. 이 경로를 우회하는 코드가 생기면 지적하세요.
   단, 서버가 입력 내용을 모르므로 "완벽한 사람이 낼 수 있는 최고점"까지는 위조가
   가능하다는 한계가 남아 있습니다(CLAUDE.md 참고). 이미 알고 있는 한계이므로
   같은 지적을 반복할 필요는 없고, 상한이 더 느슨해지는 변경만 지적하세요.

6. **Firestore 비용·지연**
   연습 세션을 Firestore에 두면 **요청 한 번이 쓰기 한 번**입니다.
   `config.KEYSTROKE_FLUSH_MS`를 줄이거나 연습 흐름에 요청을 추가하는 변경은
   쓰기 횟수를 그대로 늘리므로, 무료 한도(하루 2만 회) 대비 계산을 요구하세요.
   기록 수가 늘면 읽기 횟수나 응답 시간이 급격히 나빠지는 쿼리 패턴인지 확인하세요.
   순위표 경로는 집계 쿼리와 `order_by` + `limit` 로 필요한 문서만 읽습니다.
   색인 경로를 쓸 때는 `store.SORT_ORDER` **전부**를 `order_by`에 넣어야 합니다
   (일부만 넣고 나머지를 파이썬에서 처리하면 동점자 경계에서 순위가 틀립니다).

7. **브라우저 예외**
   null 참조, 응답 형식 가정, 이벤트 리스너 중복 등록 등으로 JS 예외가 날 수 있는
   경로를 확인하세요. 예외가 나면 학생이 기록을 저장할 수 없습니다.

8. **테스트**
   바뀐 동작을 `tests/`가 검증하는지 확인하고, 빠진 게 있으면 어떤 테스트가 필요한지
   한 줄로 제안하세요.

### 출력 규칙

- 한국어로 작성합니다.
- 심각도(**높음** / 보통 / 낮음)를 붙이고 `파일:줄번호` 형식으로 위치를 밝힙니다.
- 각 지적은 "무엇이 문제인지 → 어떤 상황에서 실제로 깨지는지 → 어떻게 고칠지"
  순서로 3줄 이내.
- 코드를 직접 확인하지 못해 추정한 내용은 **(추측)** 이라고 표시합니다.
- 스타일·포맷 취향은 지적하지 않습니다. 동작·보안·유지보수에 영향이 있는 것만.
- 문제가 없으면 "지적할 만한 문제를 찾지 못했습니다"라고 씁니다.
  **억지로 지적을 만들지 마세요.**
