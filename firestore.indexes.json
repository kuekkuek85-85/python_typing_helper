{
  "comment": "복합 색인은 필수가 아니다. 없으면 앱이 모드별 전체 읽기로 자동 대체된다(store.py의 _fetch_head). 만들어 두면 순위표 Top10과 탭 배지가 전체 문서를 읽지 않고 상위 수십 개만 읽으므로 기록이 쌓여도 비용과 응답 시간이 늘지 않는다. 기록이 수백 건을 넘어가면 만드는 것을 권한다. Firebase 콘솔 → Firestore → 색인 → 복합 색인 추가, 또는 firebase deploy --only firestore:indexes",
  "indexes": [
    {
      "collectionGroup": "records",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "mode", "order": "ASCENDING" },
        { "fieldPath": "score", "order": "DESCENDING" },
        { "fieldPath": "accuracy", "order": "DESCENDING" },
        { "fieldPath": "wpm", "order": "DESCENDING" },
        { "fieldPath": "created_at", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
